export enum Gender {
  MALE = 'Male',
  FEMALE = 'Female',
}

export enum Role {
  ADMIN = 'Admin',
  CUSTOMER = 'Customer',
  DRIVER = 'Driver',
  HOTLINE = 'Hotline',
}
export enum CustomerType {
  REGULAR = 'Regular',
  VIP = 'Vip',
}

export enum roleParam {
  ADMIN = 'admin',
  CUSTOMER = 'customer',
  DRIVER = 'driver',
  HOTLINE = 'hotline',
}
